const enteredUserName = "Jerika";

// if (enteredName.length > 0) {
if (enteredName) {
  // this code and the above code both yields to true
  console.log("Input is valid!");
}

// empty strings (""), 0, false, null, undefined are considered falsy values
// values that are not of those above (like true) are truthy values
